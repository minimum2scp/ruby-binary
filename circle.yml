machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.1.5
  services:
    - docker

general:
  artifacts:
    - files/binary
    - files/log

checkout:
  post:
    - find . -name .gitkeep -delete

dependencies:
  override:
    - rake build:prepare

test:
  override:
    - rake build:all:
        parallel: true

