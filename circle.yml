machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.1.5
  services:
    - docker

test:
  override:
    - rake
    - cp -a files/binary files/log ${CIRCLE_ARTIFACTS}

deployment:
  production:
    branch: master
    commands:
      - go get github.com/tcnksm/ghr
      - rake release[v0.0.1]
